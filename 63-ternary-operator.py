# Ternary Operator you can use this  as a shortcut in a logical conditional 
# Also called a conditional expression because evaluates a value based on a condition if is tru or not

# condition_if_true if condition else condition_if_else

is_friend = True
can_message = "message allowed" if is_friend else "not allowed to message"

print(can_message)